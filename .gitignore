# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
.vs/
*.swp
*.swo
*~

# Env (if you add .env later)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Optional: keep template folders but ignore large binaries if any
# *.zip

# Runtime-written site data (server owns these; tracking them causes cPanel
# deploy merge conflicts on every pull). Applies to all sites under sites/.
sites/*/assets/configs/feedback.json
sites/*/assets/configs/manifest.json
